#!/usr/bin/sh

# generates UUID based markdown files with prefilled YAML frontmatter

filename=$HOME/zk/$(uuidgen).md # requires uuidgenerator
frontmatter="# Note Title\ntags: #untagged \n\n"

echo $frontmatter >> $filename
subl $filename
